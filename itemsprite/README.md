# Item sprites

Upload each item sprite as a PNG using the exact stable `ItemKey` as the filename.

Examples:

- `Drinking Water.png`
- `Slime Fluid.png`
- `Iron Ingot.png`

The Items wiki automatically loads `itemsprite/<ItemKey>.png`. Missing sprites stay hidden until the correctly named PNG is uploaded.
