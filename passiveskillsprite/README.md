# Passive Skill sprites

Upload PNG sprites here using the stable `PassiveSkillKey` as the filename.

Example: `Martial Gift.png` -> `passiveskillsprite/Martial Gift.png`. Missing sprites are hidden by the wiki until the matching PNG is available.
